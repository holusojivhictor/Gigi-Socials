name: gigi_socials
description: Welcome to Gigi Socials
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^2.2.0
  firebase_auth: ^1.4.1
  firebase_core: ^1.3.0
  google_sign_in: ^5.0.4
  provider: ^5.0.0
  google_fonts: ^2.0.0
  lint: ^1.5.3
  flutter_rating_bar: ^4.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  assets:
    - assets/pictures/


  uses-material-design: true

